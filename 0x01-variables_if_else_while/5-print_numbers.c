#include <stdio.h>

/** main - main entry block
 *
 * Return: always return 0
 */

int main(void)
{
	int num;

	for (num =0; num < 10;num++)
	{
		printf("%i", num);
	}
	putchar('\n');
	return (0);
}

